# Oracle SQL Recap & Handbook
---
## 1. Database (DB)

A **Database** is an organized collection of related data.

```
Database
   ↓
Schemas
```

---

## 2. Schema

A **Schema** is a logical container that stores database objects. Each Oracle user has their own schema.

```
Database
   ↓
HR Schema
   ↓
Objects
```

---

## 3. Database Objects

Objects stored inside a schema.

**Important**
- Table
- View
- Index
- Materialized View (MVIEW)
- Global Temporary Table (GTT)

**Other Objects**
- Sequence
- Procedure
- Function
- Trigger
- Package
- Synonym
- Database Link

---

## 4. Table

A table stores actual data.

```
EMPLOYEES

EMP_ID | FIRST_NAME | SALARY
-----------------------------
101    | John       | 50000
102    | Alice      | 60000
```

- Row = Record
- Column = Field / Attribute

---

## 5. Data Types

| Data Type | Stores                           |
|-----------|-----------------------------------|
| NUMBER    | Numbers                          |
| VARCHAR2  | Variable-length text              |
| CHAR      | Fixed-length text                 |
| DATE      | Date & Time                       |
| TIMESTAMP | Date & Time + Fractional Seconds  |

---

## 6. CHAR vs VARCHAR2

**CHAR** — fixed length, pads with spaces
```sql
CHAR(5)  'Ben'  →  'Ben  '
```

**VARCHAR2** — variable length, stores only required characters
```sql
VARCHAR2(5)  'Ben'  →  'Ben'
```

Use `VARCHAR2` for most text columns.

---

## 7. DATE vs TIMESTAMP

**DATE** — Year, Month, Day, Hour, Minute, Second
```
28-JUL-2026 10:30:45
```

**TIMESTAMP** — everything in DATE + Fractional Seconds
```
28-JUL-2026 10:30:45.123456
```

---

## 8. SQL Clause Execution Order

```
SELECT
FROM
WHERE
GROUP BY
HAVING
ORDER BY
```

```sql
SELECT FIRST_NAME
FROM EMPLOYEES
WHERE SALARY > 5000
ORDER BY SALARY;
```

---

## 9. Common Oracle Errors

| Error     | Meaning              |
|-----------|-----------------------|
| ORA-00904 | Invalid column         |
| ORA-00942 | Table doesn't exist    |
| ORA-00936 | Missing expression     |
| ORA-00933 | SQL syntax error       |
| ORA-01722 | Invalid number         |
| ORA-01400 | Cannot insert NULL     |

---

## 10. SELECT Statement

```sql
SELECT column_name
FROM table_name;
```

```sql
SELECT *
FROM EMPLOYEES;

SELECT FIRST_NAME, SALARY
FROM EMPLOYEES;
```

---

## 11. DISTINCT

Removes duplicate values.

```sql
SELECT DISTINCT FIRST_NAME
FROM EMPLOYEES;
-- 107 rows -> 91 distinct

SELECT DISTINCT FIRST_NAME, LAST_NAME
FROM EMPLOYEES;
-- distinct considers ALL listed columns together
```

---

## 12. CONCAT / `||`

Joins strings.

```sql
SELECT CONCAT(FIRST_NAME, LAST_NAME)
FROM EMPLOYEES;

-- CONCAT only takes 2 arguments
SELECT FIRST_NAME, LAST_NAME, SALARY, CONCAT(FIRST_NAME, LAST_NAME, SALARY)
FROM EMPLOYEES;
-- ERROR: invalid number of arguments

-- || has no argument limit
SELECT FIRST_NAME || ' ' || LAST_NAME || '-' || SALARY
FROM EMPLOYEES;
```

---

## 13. Alias

Temporary column/table name.

```sql
SELECT FIRST_NAME AS NAME, LAST_NAME FATHER_NAME
FROM EMPLOYEES;
```

Table alias:
```sql
SELECT *
FROM EMPLOYEES E;
```

---

## 14. DUAL Table

Oracle's dummy table — exactly **1 row, 1 column** (`DUMMY` = `X`). Used for calculations/testing expressions without a real table.

```sql
SELECT *
FROM DUAL;

SELECT 10+2
FROM DUAL;

SELECT 10, 'AIMORE'
FROM DUAL;
```

> `SELECT 10+2 FROM EMPLOYEES;` runs once per row in EMPLOYEES — use `DUAL` instead for a single result.

---

## 15. ORDER BY

Sorts data. Comes at the **end** of the query.

```sql
SELECT * FROM EMPLOYEES ORDER BY SALARY ASC;
SELECT * FROM EMPLOYEES ORDER BY SALARY DESC;
SELECT * FROM EMPLOYEES ORDER BY SALARY; -- ASC by default
SELECT * FROM EMPLOYEES ORDER BY DEPARTMENT_ID, FIRST_NAME ASC;
SELECT * FROM EMPLOYEES ORDER BY DEPARTMENT_ID DESC, FIRST_NAME DESC;
```

---

## 16. Relational Operators

| Operator     | Meaning                |
|--------------|--------------------------|
| =            | Equal                    |
| <> / !=      | Not Equal                 |
| >            | Greater Than              |
| <            | Less Than                 |
| >=           | Greater Than or Equal     |
| <=           | Less Than or Equal        |
| IN           | Match any value           |
| NOT IN       | Not in list                |
| BETWEEN      | Range                      |
| NOT BETWEEN  | Outside range              |
| IS NULL      | NULL values                |
| IS NOT NULL  | Not NULL                    |
| LIKE         | Pattern search              |
| NOT LIKE     | Opposite                    |

```sql
SELECT * FROM EMPLOYEES WHERE EMPLOYEE_ID = 100;
SELECT * FROM EMPLOYEES WHERE SALARY BETWEEN 10000 AND 17000;
SELECT * FROM EMPLOYEES WHERE EMPLOYEE_ID IN (100, 105, 110, 300);
SELECT * FROM EMPLOYEES WHERE COMMISSION_PCT IS NULL;
```

> ℹ️ NULL is an *undefined* value — use `IS NULL` / `IS NOT NULL`, never `= NULL`.

---

## 17. LIKE

Pattern matching.

| Wildcard | Meaning                  |
|----------|----------------------------|
| `%`      | Zero or many characters     |
| `_`      | Exactly one character       |

```sql
WHERE FIRST_NAME LIKE 'S%'     -- starts with S
WHERE FIRST_NAME LIKE '%n'     -- ends with n
WHERE FIRST_NAME LIKE '%MEN%'  -- contains MEN
WHERE FIRST_NAME LIKE '_a%'    -- 2nd letter is 'a'
WHERE FIRST_NAME LIKE '___'    -- exactly 3 characters
```

---

## 18. Logical Operators

**AND** — both conditions must be true
```sql
WHERE SALARY > 10000 AND DEPARTMENT_ID = 90;
```

**OR** — at least one condition must be true
```sql
WHERE SALARY > 10000 OR DEPARTMENT_ID = 90;
```

| Operator | Combination      | Result |
|----------|-------------------|--------|
| AND      | TRUE AND TRUE      | Result |
| AND      | TRUE AND FALSE     | FALSE  |
| OR       | TRUE OR TRUE       | Result |
| OR       | TRUE OR FALSE      | Result |
| OR       | FALSE OR FALSE     | FALSE  |

---

## 19. Single Row Functions

Works **one row at a time**.

```
Input: Steven → UPPER() → STEVEN
```

### Number Functions

| Function | Purpose         | Example                          |
|----------|------------------|-----------------------------------|
| ROUND    | Round value       | `ROUND(10.45)`                    |
| FLOOR    | Lower integer      | `FLOOR(10.999999)` → `10`         |
| CEIL     | Upper integer      | `CEIL(10.001)` → `11`             |
| MOD      | Remainder          | `MOD(10, 2)` → `0`                |
| ABS      | Absolute value     | `ABS(-3457)` → `3457`             |
| SIGN     | Sign of number     | `SIGN(-345678)` → `-1`            |
| TRUNC    | Remove decimals    | `TRUNC(15.4567, 2)` → `15.45`     |

```sql
SELECT EMPLOYEE_ID, MOD(EMPLOYEE_ID, 2)
FROM EMPLOYEES
WHERE MOD(EMPLOYEE_ID, 2) = 1; -- odd employee IDs
```

### Character Functions

| Function  | Purpose                             |
|-----------|---------------------------------------|
| UPPER     | Uppercase                              |
| LOWER     | Lowercase                              |
| INITCAP   | First letter of each word uppercase     |
| LENGTH    | Count characters                        |
| REVERSE   | Reverse the string                      |
| REPLACE   | Replace a word/char with another        |
| TRANSLATE | Char-by-char replacement                |
| LPAD/RPAD | Pad left / pad right                    |
| TRIM      | Remove leading & trailing spaces        |
| LTRIM     | Remove left spaces                      |
| RTRIM     | Remove right spaces                     |
| INSTR     | Find position of a pattern              |
| SUBSTR    | Extract part of a string                |

```sql
SELECT REPLACE(FIRST_NAME, 'e', '*') FROM EMPLOYEES;

SELECT TRANSLATE('STEVEN', 'AEIOU', '12345') FROM DUAL;

SELECT LPAD(EMPLOYEE_ID, 6, '0') FROM EMPLOYEES;
SELECT RPAD(EMPLOYEE_ID, 6, '0') FROM EMPLOYEES;

-- SUBSTR(input, start_position, length)
SELECT SUBSTR('ORACLE DATABASE', 1, 3) FROM DUAL;   -- 'ORA'
SELECT SUBSTR('ORACLE DATABASE', 4, 7) FROM DUAL;   -- 'CLE DAT'
SELECT SUBSTR('ORACLE DATABASE', 4) FROM DUAL;      -- 'CLE DATABASE'
SELECT SUBSTR('ORACLE DATABASE', -5, 3) FROM DUAL;  -- 'ABA' (from the end)

-- INSTR(input, pattern, start_position, occurrence)
SELECT INSTR('ORACLE DATABASE', 'DATA', 1, 1) FROM DUAL;
SELECT INSTR('ORACLE DATABASE', 'A', 1, 2) FROM DUAL; -- 2nd occurrence of 'A'
```

### Date Functions

| Function       | Purpose                |
|-----------------|--------------------------|
| ADD_MONTHS      | Add/subtract months       |
| MONTHS_BETWEEN  | Difference in months      |
| LAST_DAY        | Last day of the month      |
| NEXT_DAY        | Next given weekday         |

```sql
SELECT SYSDATE, ADD_MONTHS(SYSDATE, 1) FROM DUAL;
SELECT SYSDATE, ADD_MONTHS(SYSDATE, -10) FROM DUAL;

SELECT FIRST_NAME, HIRE_DATE,
       ROUND(MONTHS_BETWEEN(SYSDATE, HIRE_DATE) / 12) AS YEARS_WORKED
FROM EMPLOYEES;

SELECT LAST_DAY(SYSDATE) FROM DUAL;
SELECT NEXT_DAY(SYSDATE, 'FRIDAY') FROM DUAL;
```

### Data Type Conversion

| Function  | Converts              |
|-----------|--------------------------|
| TO_NUMBER | String → Number           |
| TO_CHAR   | Number/Date → String       |
| TO_DATE   | String → Date               |
| CAST      | Convert any datatype        |

```sql
SELECT TO_NUMBER('10') FROM DUAL;
SELECT TO_CHAR(SYSDATE, 'DD-MON-YYYY') FROM DUAL;
SELECT TO_CHAR(SYSDATE, 'DAY') FROM DUAL;
SELECT TO_DATE('15-06-2026', 'DD-MM-YYYY') FROM DUAL;

SELECT CAST('10' AS NUMBER) FROM DUAL;
SELECT CAST('10-12-2025' AS DATE) FROM DUAL;
SELECT CAST(65456 AS CHAR(10)) FROM DUAL;
```

###  General Functions

| Function | Purpose                | Syntax                                |
|----------|--------------------------|------------------------------------------|
| NVL      | Replace NULL              | `NVL(input, null_replace)`               |
| NVL2     | If NULL / Not NULL         | `NVL2(input, value_if_not_null, value_if_null)` |
| COALESCE | First non-NULL value       | `COALESCE(v1, v2, v3, ...)`              |
| NULLIF   | NULL if two values equal   | `NULLIF(a, b)`                            |

```sql
SELECT COMMISSION_PCT, NVL(COMMISSION_PCT, 0)
FROM EMPLOYEES;

SELECT FIRST_NAME, SALARY,
       NVL2(COMMISSION_PCT, 0.1, 0.05) AS COMMISSION_RATE,
       SALARY + (SALARY * NVL2(COMMISSION_PCT, 0.1, 0.05)) AS FINAL_AMOUNT
FROM EMPLOYEES;

SELECT COALESCE(COMMISSION_PCT, MANAGER_ID, DEPARTMENT_ID)
FROM EMPLOYEES; -- returns first non-null column

SELECT NULLIF(10, 10) FROM DUAL; -- NULL
SELECT NULLIF(10, 20) FROM DUAL; -- 10
```

---

## 20. Set Operators

Combine 2+ `SELECT` statements.

**Rules**
- Same number of columns in every SELECT
- Same (or compatible) data types in matching positions

| Operator   | Behavior                                 |
|------------|---------------------------------------------|
| UNION      | Merge rows, remove duplicates, sorted result |
| UNION ALL  | Merge rows, keep duplicates, no sorting       |
| INTERSECT  | Return only common rows                        |
| MINUS      | Return rows in first query but not the second   |

```sql
-- T1: 1,2,3,4    T2: 3,4,5,6

SELECT * FROM T1
UNION
SELECT * FROM T2;
-- 1,2,3,4,5,6

SELECT * FROM T1
UNION ALL
SELECT * FROM T2;
-- 1,2,3,4,3,4,5,6

SELECT * FROM T1
INTERSECT
SELECT * FROM T2;
-- 3,4

SELECT * FROM T1
MINUS
SELECT * FROM T2;
-- 1,2
```

 Column count mismatch:
```sql
SELECT EMPLOYEE_ID, FIRST_NAME, SALARY FROM EMPLOYEES WHERE DEPARTMENT_ID = 90
UNION
SELECT DEPARTMENT_ID, DEPARTMENT_NAME FROM DEPARTMENTS WHERE DEPARTMENT_ID IN (10,20,30);
--  ORA-00913 / query block has incorrect number of result columns
```

 Data type mismatch at a given position:
```sql
SELECT EMPLOYEE_ID, FIRST_NAME FROM EMPLOYEES WHERE DEPARTMENT_ID = 90
UNION
SELECT DEPARTMENT_NAME, DEPARTMENT_ID FROM DEPARTMENTS WHERE DEPARTMENT_ID IN (10,20,30);
--  expression must have same datatype as corresponding expression
-- (2nd column: VARCHAR2 vs NUMBER mismatch)
```

---

##  Mental Model

```
Database
│
├── Schema
│      │
│      ├── Table
│      ├── View
│      ├── Index
│      ├── MVIEW
│      └── GTT
│
└── Another Schema
```

```
EMPLOYEES
EMP_ID | FIRST_NAME | SALARY

EMP_ID         NUMBER
FIRST_NAME     VARCHAR2(50)
SALARY         NUMBER(10,2)
HIRE_DATE      DATE
```

---

